// @aeturnis/shared - Shared types and utilities
// This package contains all shared code between server and client

export const GAME_VERSION = '1.0.0';

// Export all auth schemas and types
export * from './schemas/auth.schemas';

// TODO(claude): Add shared types for accounts, characters, items, etc.
// TODO(claude): Add more shared validation schemas using Zod
// TODO(claude): Add shared constants and enums